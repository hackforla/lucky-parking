import { PropsWithChildren } from "react";

export default function CitationExplorerSectionTitle({ children }: PropsWithChildren) {
  return <div className="heading-5 text-black-500 uppercase">{children}</div>;
}
