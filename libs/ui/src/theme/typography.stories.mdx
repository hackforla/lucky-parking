import _ from 'lodash';
import { Meta, Story } from '@storybook/addon-docs';
import { styled } from '@storybook/theming';
import typography from '../tokens/typography';

<Meta title="Theme/Typography" />

<Story name="Large (Desktop/Laptop)">
    {_.map(typography.large, (fontStyles, typographyName) => (
        <div 
            key={typographyName}
            style={{
                fontFamily: `${fontStyles.fontFamily}`,
                fontSize: `${fontStyles.fontSize}`, 
                fontWeight: `${fontStyles.fontWeight}`,
                lineHeight: `${fontStyles.lineHeight}`,
                letterSpacing: `${fontStyles.letterSpacing}`,
            }}
        >
            {typographyName}
        </div>
    ))}
</Story>

<Story name="Small">
    {_.map(typography.small, (fontStyles, typographyName) => (
        <div 
            key={typographyName}
            style={{
                fontFamily: `${fontStyles.fontFamily}`,
                fontSize: `${fontStyles.fontSize}`, 
                fontWeight: `${fontStyles.fontWeight}`,
                lineHeight: `${fontStyles.lineHeight}`,
                letterSpacing: `${fontStyles.letterSpacing}`,
            }}
        >
            {typographyName}
        </div>
    ))}
</Story>

