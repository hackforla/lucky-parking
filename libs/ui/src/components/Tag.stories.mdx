import { Meta, Story } from '@storybook/addon-docs';
import Tag from './Tag';

<Meta title="Components/Tags" component={Tag} />

export const Template = (args) => <Tag {...args} />;

<Story
    name="Blue"
    args={{
        color: 'blue',
        children: 'Place (Radius)',
    }}>
    {Template.bind({})}
</Story>

<Story
    name="Purple"
    args={{
        color: 'purple',
        children: 'Zip Code',
    }}>
    {Template.bind({})}
</Story>

<Story
    name="Green"
    args={{
        color: 'green',
        children: 'Neighborhood Council',
    }}>
    {Template.bind({})}
</Story>